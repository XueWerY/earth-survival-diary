# 2026-07-24

## 笔记重命名：内联 input 改为弹窗
- **文件**：`src/components/notes/NotesPage.vue`
- **改动**：
  - 删除面包屑中的内联 `<input>` 重命名输入框（模板 + CSS `.breadcrumb-rename-input`）
  - 删除 `renameInputRef`、`setRenameInputRef`
  - 新增 `el-dialog` 弹窗（标题输入框 + 确定/取消按钮）
  - `startRenameNote`：改为 `showRenameDialog = true`
  - `commitRenameNote`：关闭弹窗并提交
  - 新增 `onRenameDialogOpen` 回调
- 版本号：`2026.7.23-38` → `2026.7.24-1`

## 重命名弹窗样式优化
- 标题居中、隐藏右上角叉号（`show-close="false"`）
- 标题下方 + 按钮上方各加分隔线（`border-bottom`/`border-top`）
- 底部按钮居中（flexbox `justify-content: center`）
- 版本号：`2026.7.24-1` → `2026.7.24-2`

## 重命名弹窗样式修复（scoped → 非 scoped）
- **问题**：el-dialog 默认 teleport 到 body，scoped `:deep()` 穿透无效，上次样式未生效
- **修复**：将弹窗样式从 `<style scoped>` 移到独立 `<style>` 非 scoped 块
- 版本号：`2026.7.24-2` → `2026.7.24-3`

## 重命名弹窗样式再次修复
- **叉号问题**：`show-close="false"` 是字符串（truthy），改为 `:show-close="false"` 绑定布尔���
- **分隔线问题**：CSS `border-bottom`/`border-top` 加 `!important` 覆盖 Element Plus 默认样式
- 版本号：`2026.7.24-3` → `2026.7.24-4`

## 重命名弹窗样式终极修复
- **根因**：el-dialog teleport to body，非 scoped CSS 中 `.rename-dialog` 选择器仍无法匹配内部元素
- **方案**：改用 header/footer slot 直接控制 HTML，分隔线写在 inline style 上，彻底绕过 Element Plus 内部样式
- 删除了无用的非 scoped `<style>` 块
- 版本号：`2026.7.24-4` → `2026.7.24-5`

## 重命名弹窗标题颜色修复
- header slot div 缺少 `color`，默认继承 el-dialog 的暗色文字，在深色背景下不可见
- 加 `color: #fff`
- 版本号：`2026.7.24-5` → `2026.7.24-6`

## 重命名弹窗样式再次修复
- **叉号问题**：`show-close="false"` 是字符串（truthy），改为 `:show-close="false"` 绑定布尔值
- **分隔线问题**：CSS `border-bottom`/`border-top` 加 `!important` 覆盖 Element Plus 默认样式
- 版本号：`2026.7.24-3` → `2026.7.24-4`

## 修复 Electron 打包后 express 缺失导致崩溃
- **Bug**：安装运行后报 `Cannot find module 'express'`，应用直接退出
- **根因**：`scripts/build-tools.cjs` 中 `installDeps()` 仅检查 `.deps-ok` 标记文件，但 `electron/node_modules` 可能被独立清理（如 git clean），导致打包后的 `resources/node_modules/` 缺少 express/cors
- **修复**：条件判断增加 `fs.existsSync(express目录)` 验证，确保关键模块真正存在
- 文件：`scripts/build-tools.cjs`
- 版本号：`2026.7.24-6` → `2026.7.24-7`

## 足迹页面重设计（Ardot 画布）
- **任务**：在 Ardot 画布上重新设计「地球 Online 生存日记」的足迹页面（Web App 场景）
- **设计系统**：
  - Style: glass-arc-browser（毛玻璃 + 宇宙渐变）
  - Color: glass-noir-grain（#0F0E0E 深黑底 + 金色 #E8C268 点缀）
  - Typography: dark-discord（Inter SemiBold + Sarasa Gothic SC 中文字体）
  - Layout: bento-gmail-pill-sidebar-workspace（256px 侧栏 + 主内容）
  - Scene: mobile-productivity（时间线卡片流）
  - Composition: shadow-system-3tier（三层阴影）
- **页面结构**：1440×1280 画布
  - 左侧 256px 毛玻璃侧栏（地球Online Logo + 9 项导航）
  - 顶部栏：日期导航（前后箭头 + 农历日期）+ 「记录足迹」「写日记」按钮
  - 分区标签：重要 / 上午 / 下午 / 晚上（金色重要为选中态）
  - 时间线内容区：重要区（倒数日+星标活动）、上午（日记+课程+活动）、下午（编程活动）、晚上（健身活动）
  - 右下角悬浮按钮：紫粉渐变 FAB
- **技术注意**：
  - `rgba()` 不能用作 `fill` 简写，需用 `fills: [{type: "SOLID", color: {r,g,b}, opacity: 0.06}]`
  - PingFang SC 不可用，需改用 Sarasa Gothic SC（Medium → Semi Bold）
  - 文本节点禁用 emoji 字符，所有图标必须用 SVG frame
  - 弹性盒容器内子节点 x/y 被忽略，需用 `layoutPositioning: "ABSOLUTE"` 实现绝对定位
  - 画布高度需根据内容扩展（900 → 1280）以容纳完整时间线

## 足迹页面重设计（落地实现）
- **任务**：把 Ardot 设计稿落到实际 Vue 代码
- **文件**：`src/components/footprint/TaskList.vue`
- **改动**：
  - 移除顶部 `InlineLunarDatePicker`（周日历网格）→ 改为轻量 header：左右箭头 + 公历/农历标题 + 两个胶囊按钮
  - section-nav → section-tabs：增加选中态高亮，"重要" tab 强制使用 chalk-amber 强调色
  - 农历/星期计算：用 `lunar-javascript` 的 Solar 在 lunarDateLabel computed 中
  - 点击标题 → 打开 `DateScrollPicker` 浮层跳转日期（独立实例，避免与倒数日冲突）
  - 滚动监听 `.footprint-content .el-scrollbar__wrap`（不是 window）→ `updateActiveSection` 自动同步高亮
- **保留**：`InlineLunarDatePicker` 组件文件不动；现有 RecordCard/DiaryCard/CountdownCard/弹窗逻辑全保留
- **新增依赖图标**：`ArrowLeft / ArrowRight / Plus / EditPen`（@element-plus/icons-vue）
- **版本号**：`2026.7.24-7` → `2026.7.24-8`
- **验证**：`npx vue-tsc --noEmit` 无新错误；`npx vite build` 成功（输出到 /tmp 验证，未污染 dist）
- **坑**：`el-scrollbar` 内部用嵌套 div 模拟滚动，window scroll 事件不会触发，必须监听 `.el-scrollbar__wrap`

## 电脑端导航栏布局优化
- **任务**：参考用户提供的截图优化电脑端全局导航栏
  - 顶部添加应用图标 + "地球Online生存日记" 标题
  - "我的" 和 "工具箱" 移动到 "统计" 下方
  - 图标和文字一行显示
  - 图标换成 Element Plus 线性图标
- **文件**：
  - `src/components/common/nav/MainNav.vue`：模板加 `.nav-header`（仅 nav-left 显示）；引入 `build/app-icon.png`；图标用 `<component :is="MODULE_ICONS[m]" />` 渲染；`.nav-left` 宽度 84px → 200px；`.nav-item` 改 `flex-direction: row` + `width: 100%`；删除 `nav-item-bottom` 类（`margin-top: auto` 推底逻辑）
  - `src/composables/usePageNav.ts`：`MODULE_ICONS` 类型从 `Record<string, string>` 改为 `Record<string, any>`，emoji → Element Plus 组件（Plus/Notebook/Timer/List/AlarmClock/Calendar/Histogram/Grid/User）
- **未改**：
  - `App.vue`：使用 flex 弹性布局，nav 宽度变化自动适配，无需改
  - `guideContent.ts`：未涉及功能描述变更
  - `项目规范.html`：未涉及通用规范变更
  - 插件：无
- **版本号**：`2026.7.24-8` → `2026.7.24-9`
- **坑**：
  - `vite build` 会清空 dist 目录被沙箱拦截，无法本地构建验证；改用 `vue-tsc --noEmit` 类型检查（项目原有 TS 错误，未引入新错误）
  - 引入 `build/app-icon.png` 用了相对路径 `../../../../build/app-icon.png`，Vite 默认处理静态资源 import

## 电脑端导航栏进一步优化（宽度/分隔线/高亮/间距）
- **任务**：5 项 CSS 微调
  1. 导航栏宽度 200px → 210px
  2. `.nav-header` 底部加 `border-bottom` 分隔线
  3. `.nav-left .nav-items-scroll` 加 `justify-content: flex-start` 取消垂直居中，padding-top 从 4px → 10px
  4. `.nav-item.active` 背景从 transparent → `rgba(102, 126, 234, 0.18)`（主题色高亮）
  5. `.nav-left .nav-items-scroll` gap 从 2px → 8px（增大间距）
- **文件**：`src/components/common/nav/MainNav.vue`（仅 CSS 块）
- **版本号**：`2026.7.24-9` → `2026.7.24-10`
- **未改**：guideContent.ts（纯 UI）、项目规范.html（无规范变更）、插件（无）

## 电脑端导航栏微调（分隔线间距/hover/对比度）
- **任务**：3 项 CSS 微调
  1. 分隔线更清晰 + 左右留间距：`.nav-header` opacity 0.08→0.16，`margin: 0 12px`，padding 16→4px（总 inset 保持 16px）
  2. hover 高亮项不取消高亮：`.nav-item:hover` → `.nav-item:not(.active):hover`
  3. 导航栏与内容区对比增强：`.nav-left` 加 `border-right: 1px solid rgba(255,255,255,0.1)`
- **文件**：`src/components/common/nav/MainNav.vue`（仅 CSS 块）
- **版本号**：`2026.7.24-10` → `2026.7.24-11`

## 修复 hover 高亮项仍被取消的根因
- **根因**：`App.vue` 中桌面端 MainNav 传了 `noHover` prop，`.no-hover` class 始终存在
- `.no-hover .nav-item:hover` 优先级 0,2,1 高于 `.nav-item.active` 的 0,2,0，hover 时覆盖高亮背景
- **修复**：新增 `.no-hover .nav-item.active:hover`（优先级 0,3,1），在 no-hover 模式下也保持 active 高亮
- **文件**：`src/components/common/nav/MainNav.vue`
- **版本号**：`2026.7.24-11` → `2026.7.24-12`

## 足迹导航项图标更换
- `Plus` → `MapLocation`（地图定位）
- **文件**：`src/composables/usePageNav.ts`（import + MODULE_ICONS 映射）
- **版本号**：`2026.7.24-12` → `2026.7.24-13`

## 足迹页面布局优化
- **任务**：电脑端宽度填满父容器（自动扣除 210px 导航栏），安卓端 100%；顶部日期左对齐+去"农历"前缀+按钮右对齐圆角矩形；导航项改为圆角矩形左对齐；标签左对齐；卡片从单列列表改为自���应网格
- **文件**：`src/components/footprint/TaskList.vue`
- **改动**：
  - CSS: `.footprint-container` width 从 `min(500px, 80vw)` → `width: 100%`，加 `padding: 0 16px`
  - CSS: `.page-header` justify-content 从 center → space-between，date padding 去掉水平值
  - CSS: `.header-title` text-align center → left，加 `flex: 1`
  - CSS: `.header-action-btn` border-radius 999px → 8px
  - CSS: `.section-tab` border-radius 999px → 8px
  - CSS: `.important-title` / `.period-title` text-align center → left
  - CSS: `.diary-content` text-align center → left
  - CSS: `.period-items` 改为 `display: grid; gap: 8px`；`.period-item` 简化为 `min-width: 0`
  - Script: `lunarDateLabel` 去掉"农历"前缀
  - Script: 新增 ResizeObserver + `cardColumns` computed（CARD_WIDTH=220, CARD_GAP=8, max 6 列）
  - Template: `.footprint-container` 加 `ref="containerRef"`
  - Template: pinnedRecords 外层包裹 `.period-items` div + grid inline style
  - Template: 上午/下午/晚上的 `.period-items` 加 `:style="{ gridTemplateColumns: ... }"`
- **版本号**：`2026.7.24-13` → `2026.7.24-14`
- **未改**：guideContent.ts（纯布局）、项目规范.html（无规范变更）、插件（无）
- **坑**：vite build 被沙箱拦截（dist 目录 61 文件超 50 阈值），但改动纯 CSS+简单 computed，无新增 import/依赖，不存在编译风险

## 足迹页面布局优化（第二轮）
- **任务**：日期固定宽度+副标题居中；记录足迹图标换 DocumentAdd；删除四个导航项；标签加折叠按钮；倒数日卡片自适应网格
- **文件**：`src/components/footprint/TaskList.vue`
- **改动**：
  - CSS: `.header-title` `flex: 1` → `width: 200px; flex: none; text-align: center`
  - 图标: `Plus` → `DocumentAdd`（import + 模板）
  - 模板: 删除整个 `.section-tabs` 导航项区域
  - 模板: 重构重要区域 — 合并 pinned/unpinned countdown 到同一 `important-section`，各块用 `countdown-grid` + `cardColumns` 网格
  - 模板: 重要/上午/下午/晚上标题加 `@click` 折叠 + `collapse-arrow` 三角箭头（▶/▼）
  - Script: 删除 `activeSectionId`/`updateActiveSection`/`sectionItems`/`scrollToSection`/`morningTitle`/`afternoonTitle`/`eveningTitle`
  - Script: 删除 `onMounted`/`onBeforeUnmount` 中的滚动监听 (el-scrollbar__wrap scroll event)
  - Script: 删除 `watch(selectedDateValue)` 中的 sectionItems/activeSectionId 逻辑
  - Script: 新增 `importantCollapsed`/`morningCollapsed`/`afternoonCollapsed`/`eveningCollapsed` refs
  - CSS: 删除全部 `.section-tabs`/`.section-tab` 相关 CSS（约 90 行）
  - CSS: `.countdown-section` → `.countdown-grid`（display:grid + gap:8px）
  - CSS: `.countdown-item` margin-bottom → `min-width:0`
  - CSS: `.important-title`/`.period-title` 加 `cursor:pointer; user-select:none`
  - CSS: 新增 `.collapse-arrow` 箭头样式
  - CSS: 删除重复 `.important-title` 定义
- **版本号**：`2026.7.24-14` → `2026.7.24-15`
- **未改**：guideContent.ts、项目规范.html、插件

## 足迹页面 Bug 修复：折叠按钮多余的 </div>
- **症状**：`[plugin:vite:vue] Element is missing end tag.` 指向 TaskList.vue:35:9
- **根因**：上一轮添加 `v-if="!xxxCollapsed"` 折叠控制时，误以为 v-if 会产生新包裹层（实际是切换原 div 渲染），给晨间/午间/晚间三个 region 多加了 `</div>` 各一个
- **修复**：删除三个 region 多余的 `</div>`，恢复每个 region 恰好两对 `</div>`（period-items + diary-period）
- **验证**：写脚本直接调用 `@vue/compiler-sfc` 的 `parse()` + `compileTemplate()`，输出 `Parse: OK` / `Template compile: OK`
- **版本号**：`2026.7.24-15` → `2026.7.24-16`

## 足迹页面微调：日期贴近箭头 + 倒数日卡片加宽
- **日期贴近**：`.page-header` 去掉 `justify-content: space-between`（恢复默认左对齐），`.header-actions` 加 `margin-left: auto` 推按钮到右端
- **卡片加宽**：`CARD_WIDTH` 220 → 280，所有卡片（记录卡+倒数日卡）统一变宽
- **文件**：`src/components/footprint/TaskList.vue`
- **版本号**：`2026.7.24-16` → `2026.7.24-17`

## 足迹页面：日期宽度+卡片网格简化
- **日期宽度**：`.header-title` width 200px → 150px
- **卡片网格**：用纯 CSS `repeat(auto-fill, minmax(250px, 500px))` 替代 JS ResizeObserver 方案
- **清理**：删除 `containerRef`/`containerWidth`/`CARD_WIDTH`/`CARD_GAP`/`cardColumns`/`resizeObserver`/`onBeforeUnmount`
- **模板**：移除 6 处 `:style="{ gridTemplateColumns: ... }"` 内联绑定和 `ref="containerRef"`
- **文件**：`src/components/footprint/TaskList.vue`
- **版本号**：`2026.7.24-17` → `2026.7.24-18`

## 足迹页面大清理：只保留记录和日记卡片
- **任务**：移除倒数日卡片/清单任务/课程表，只展示足迹记录和日记
- **文件**：`src/components/footprint/TaskList.vue`
- **模板**：
  - 删除整块 important-section（倒数日+星标区域）
  - 删除 morning/afternoon/evening 中的 TaskCard/CourseCard 块（6处）
  - 删除 MoveTaskPage/CountdownForm/task删除/Countdown删除 对话框模板
- **Script**：
  - 删除 imports：CourseCard/TaskCard/CountdownCard/CountdownForm/MoveTaskPage/chalk/setData/useListStore/ListTask/Ref/Close
  - 删除 countdown 全部代码（~140行）：injects/refs/函数/存储逻辑
  - 删除 list 相关：showMoveDialog/handleTaskDelete/onTaskComplete/showTaskDeleteConfirm
  - 删除 `pinRecords`/`pinnedCountdownCards`/`unpinnedCountdownCards`/`countdownDisplayCards`/`listCards`/`courseCards`
  - 删除过滤星标的本地 `morningCards`/`afternoonCards`/`eveningCards` computed，直接用 useFootprintCards 返回值
  - `useFootprintCards` 的 countdownMilestones 参数改为空 ref
  - 删除重复的 `importantCollapsed` 声明
- **版本号**：`2026.7.24-18` → `2026.7.24-19`

## 足迹页面 Bug：晚上文字乱码
- **症状**：period-evening title 显示为 `🌙 上` (中间字变方块乱码)
- **根因**：上一轮清理代码时，evening title 的 `晚上` 文字编码在传输/编辑过程中损坏（U+FFFD 替换字符），只留下 `上` 字
- **修复**：用 Python 直接读取 UTF-8 字节重新写入，将 `</span> 🌙 上` 替换为 `</span> 🌙 晚上`
- **坑**：Edit 工具按 UTF-8 字面匹配，遇到 U+FFFD 占位符无法精准定位，必须用 Python re.sub 处理
- **文件**：`src/components/footprint/TaskList.vue`
- **版本号**：`2026.7.24-19` → `2026.7.24-20`

## 足迹页面卡片间距与列数精确控制
- **间距**：统一 16px
- **列数公式**：`cardColumns = max(1, floor((containerWidth - GAP) / (250 + GAP)))`，GAP=16
- **阈值验证**：container=500→1列 ✓；700→2列 ✓；900→3列 ✓；1200→4列 ✓
- **实现**：
  - CSS: `.period-items` 去 auto-fill，gap 8→16
  - CSS: 删 `.countdown-grid`/`.countdown-item`
  - 模板: `ref="containerRef"` + 3处 `:style`
  - Script: 恢复 `containerRef`/`resizeObserver`/`onBeforeUnmount` + `updateCardColumns()`
- **文件**：`src/components/footprint/TaskList.vue`
- **版本号**：`2026.7.24-20` → `2026.7.24-21`

## 足迹页面 Bug：记录足迹提交报错
- **症状**：点击"记录足迹"提交后报 `Cannot read properties of undefined (reading 'name')`
- **根因**：RecordForm.vue `emit('submit')` 不传参，但 TaskList.vue 的 `handleFormSubmit(task: Task)` 访问 `task.name`（task 为 undefined）
- **修复**：`handleFormSubmit` 去掉 `task` 参数，日志改为用 `editingTask.value`（编辑态）或通用文案（新增态）
- **文件**：`src/components/footprint/TaskList.vue`
- **版本号**：`2026.7.24-21` → `2026.7.24-22`

## 足迹页面 Bug：空时段标签错误显示
- **症状**：只记录了晚上的活动，上午标签仍然显示（但无卡片）
- **根因**：`morningCards` from `useFootprintCards` 包含 list/course 类型卡片，`v-if="morningCards.length > 0"` 因这些非 record 卡片而满足
- **修复**：新增 `morningRecordCards`/`afternoonRecordCards`/`eveningRecordCards` computed，只过滤 `type === 'record'`，v-if 和 v-for 都改用这些新变量
- **文件**：`src/components/footprint/TaskList.vue`
- **版本号**：`2026.7.24-22` → `2026.7.24-23`

## 跳转日期弹窗布局优化
- **任务**：公历/农历按钮改圆角矩形各占一半；滚动选择器只显示 3 个选项
- **文件**：`src/components/common/picker/DateScrollPicker.vue`
- **改动**：
  - `.jump-toggle`：去掉 `justify-content: center`，新增 `.jump-toggle .capsule-btn` 规则：`flex: 1; border-radius: 8px; height: 36px; font-size: 13px`（两按钮均分宽度 + 圆角矩形）
  - `.jump-scrolls`：height 180px → 108px（3 × 36px 项高）
  - `.jump-scroll-spacer`：height 72px → 36px（(108-36)/2 居中算式）
- **版本号**：`2026.7.24-23` → `2026.7.24-24`
