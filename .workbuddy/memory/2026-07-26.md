# 2026-07-26 工作日志

## 修翻页底半倒置 BUG（v2026.7.26-60）
- .fd-old/.fd-new 加 backface-visibility:hidden

## 清理翻页时钟冗余 CSS（v2026.7.26-58）
- 删除之前残留的 f-top/f-bottom/f-unit/f-sep/f-sep-inner 等无效规则
- 只保留 .fc-* 新命名空间
- 版本：2026.7.26-57 → 2026.7.26-58

## 参考 flip-watch 重写（v2026.7.26-57）
- 三层结构：back(新值隐藏)+up(旧值上半)+low(新值下半 translateY(-50%))
- up 旋转 0→-90° 0.3s，动画后换内容+瞬间复位
- reference: alexn256/flip-watch

## 落片方向 0→90°（v2026.7.26-55）
- fall-down -90°→90°，落片向外翻出
- 版本：2026.7.26-54 → 2026.7.26-55

## 参考标准翻页实现重写（v2026.7.26-54）
- 参考 FlipClock 设计方案，四层结构：f-up(静态上) + f-dn(静态下) + f-fall(落片) + f-rise(升片)
- 落片 f-fall：旧数上半，绕底边 0→-90° 0.3s ease-in 消失
- 升片 f-rise：新数下半，绕顶边 90°→0 0.3s ease-out 延迟0.3s 出现
- 静态上/下始终显示新数字
- 版本：2026.7.26-53 → 2026.7.26-54

## 翻页改为"从屏幕里翻出来"（v2026.7.26-53）
- .f-inner 默认 rotateX(-85deg) 藏在屏幕里（后倾不可见）
- 翻页时 rotateX(0) 从屏幕里弹出来变平展
- 静态 f-top 始终显示当前数字，翻片只负责覆盖式的"浮现"效果
- 版本：2026.7.26-52 → 2026.7.26-53

## 翻转方向改为正向（v2026.7.26-52）
- rotateX(-85deg) → rotateX(85deg)，翻片从屏幕里往外翻
- 版本：2026.7.26-51 → 2026.7.26-52

## 透视内联到 transform（v2026.7.26-51）
- perspective 作为函数写进 transform 值：perspective(300px) rotateX(-85deg)
- transform-origin 改用 50% 100% 明确底边 pivot
- 版本：2026.7.26-50 → 2026.7.26-51

## 透视移到外层+调整视角（v2026.7.26-50）
- perspective 从 .f-top 移到 .f-unit（300px），perspective-origin 50% 25%
- .f-top 去掉透视，让相机从卡片上方偏左看
- 版本：2026.7.26-49 → 2026.7.26-50

## 翻页改用两段rotateX（v2026.7.26-49）
- scaleY 改为 rotateX(-85deg)：上片绕铰链向外翻到 85°→换内容→自动弹回 0°
- .f-top 带 perspective:200px，.f-inner 转 rotateX
- 版本：2026.7.26-48 → 2026.7.26-49

## 翻页用scaleY替代rotateX（v2026.7.26-48）
- 删除 3D rotateX/backface-visibility/perspective，改用 scaleY(1→0→1)
- 收缩 0.12s ease-in，200ms后更新内容，自动展开 0.3s ease-out
- 只留单面 .f-inner，不用背面前后面
- 版本：2026.7.26-47 → 2026.7.26-48

## 修翻页方向+透视（v2026.7.26-47）
- 回到 rotateX(-180deg) 向外翻
- perspective 从 .f-unit 移到 .f-top（400px），避免整体透视扭曲
- 版本：2026.7.26-46 → 2026.7.26-47

## 修翻页方向：往里翻→往外翻（v2026.7.26-46）
- rotateX(-180deg) → rotateX(180deg)，上片向外翻开而非向内翻入
- 版本：2026.7.26-45 → 2026.7.26-46

## 翻页时钟彻底重构（v2026.7.26-45）
- 删除全部旧 CSS，用经典 .f-inner 翻转模式：上下半片各 50% 等高，overflow:hidden 裁剪
- 翻片 .f-inner 在 f-top 内，transform-origin: bottom center，front/back 各占一面
- 文字 .f-txt 200% 高，top:0 显示上半/top:-100% 显示下半
- 删除之前的 calc 缝隙，用等分 50% 自然留出中线视觉空隙
- 版本：2026.7.26-44 → 2026.7.26-45

## 修翻页下半偏移 BUG（v2026.7.26-44）
- line-height: 84px → 1，flex align-items: center 让文字自然居中
- 之前 line-height 大于 font-size 导致文字基线偏移，下半片出现重影/偏移
- 版本：2026.7.26-43 → 2026.7.26-44

## 修翻页双影 BUG（v2026.7.26-43）
- .flip-flap-face 统一 backface-visibility: hidden——背面 rotateX(180deg) 后背面朝观察者，父级翻 -180° 后背面翻转重新可见
- 版本：2026.7.26-42 → 2026.7.26-43

## 修翻页背面倒置 BUG（v2026.7.26-42）
- .flip-flap 高度 50%（覆盖 top 静态半片+1px 间隙），transform-origin: center bottom 自然在卡片正中央
- .flip-flap-face 去掉统一 backface-visibility，只对 .flip-flap-front 应用——否则翻到位后背面又被自己隐藏
- 版本：2026.7.26-41 → 2026.7.26-42

## 修翻片跑偏 BUG（v2026.7.26-41）
- transform-origin bottom→center calc(100%+1px)，铰链点定位到卡片中央 50% 处
- 翻片高度是 50%-1px，底部在 50%-1px，原点+1px 刚好回到卡片正中间
- 版本：2026.7.26-40 → 2026.7.26-41

## 翻页时上下半同时更新（v2026.7.26-40）
- 删除 bottomChars 滞后机制，flip-bottom 直接用 ch（新数字）
- 翻页时上半翻转+下半立即更新为同步
- 版本：2026.7.26-39 → 2026.7.26-40

## 数字切缝 4px→2px（v2026.7.26-39）
- calc(50%-2px) → calc(50%-1px)，缝隙从4px缩到2px
- 版本：2026.7.26-38 → 2026.7.26-39

## 数字中间切缝（v2026.7.26-38）
- 上半片/下半片/翻片高度 50%→calc(50%-2px)，中间留4px缝隙
- 删除 ::after 画线方案
- 版本：2026.7.26-37 → 2026.7.26-38

## 翻页数字中间加铰链线（v2026.7.26-37）
- .flip-unit::after 1px 半透白横线，居中 top:50%，z-index:3
- 分隔符不显示
- 版本：2026.7.26-36 → 2026.7.26-37

## 翻页时钟去卡片只剩下翻转数字（v2026.7.26-36）
- flip-half/flip-flap-face 去所有背景/边框/阴影/圆角，只留透明+overflow裁剪
- 版本：2026.7.26-35 → 2026.7.26-36

## 翻页卡片可见性+小时+去冒号（v2026.7.26-35）
- 卡片背景 7%→10%，边框 14%→18%，数字 60→64px，对比度显著提升
- displayTime 始终 hh mm ss 格式，空格替代冒号
- 冒号样式 .is-colon → .is-sep，宽度 16px
- 版本：2026.7.26-34 → 2026.7.26-35

## 修翻页不翻 BUG：transform:none 卡死（v2026.7.26-34）
- 复位时 e.style.transform='none' 是内联样式，会永久挡住 CSS .flip 的 rotateX(-180deg)
- 改为 e.style.transform=''（空串，移除内联样式），让 CSS 重新接管
- 版本：2026.7.26-33 → 2026.7.26-34

## 翻页时钟视觉+动画+布局改版（v2026.7.26-33）
- 卡片 44×74 → 52×84px，数字 56→60px，间距 4→6px
- 背景改渐变 + inset 高光 + 外投影，质感提升
- 翻片动画 0.6s → 0.5s，easing 调为更有重力感的曲线
- 版本：2026.7.26-32 → 2026.7.26-33

## 翻页动画修复：上半先翻下半后更新（v2026.7.26-32）
- 新增 bottomChars ref，滞后于 timeChars 一个动画周期
- 翻页时 flip-bottom 显示 bottomChars（旧数字），600ms 后和 prevTimeChars 一起同步为新值
- 版本：2026.7.26-31 → 2026.7.26-32

## 翻页时钟 3D→2D（v2026.7.26-31）
- 用户要求回退，移除 FocusTimerScene 3D 组件，恢复 CSS flip clock
- 恢复 flip HTML、CSS、DOM querySelector 复位逻辑
- 版本：2026.7.26-30 → 2026.7.26-31

## Three.js 3D 翻页时钟（v2026.7.26-30）
- FocusTimerScene.vue 重写为 3D 翻页时钟：
  - 每个数字：底部静态半片 + 上部翻片（铰链 pivot）+ 后部静态半片
  - Canvas纹理：128×64，top/bottom 对齐渲染数字半身
  - 翻片 pivot 绕 X 轴 rotate -PI*eased，ease-out，0.55s
  - 动画后复位纹理
- FocusTimer.vue：flip 模式用 FocusTimerScene 替代 CSS flip clock
- 删除全部 CSS flip clock 样式和 DOM 查询逻辑
- 版本：2026.7.26-29 → 2026.7.26-30

## 修翻页时钟三点 BUG（v2026.7.26-29）
- 原 card-top/card-bottom 用 flex 居中+内 span 无高度→ 文字溢出后只露三个点
- 重构为 flip-half (position absolute, height 50%, overflow hidden) + flip-text (height 200%, line-height 74px)
- 上半片 top:0 显示上半身，下半片 top:-100% 显示下半身
- 翻片 .flip-flap-front/back 同样 200% 高度的 flip-text
- 版本：2026.7.26-28 → 2026.7.26-29

## 翻页时钟改为上下分割+上半片下翻（v2026.7.26-28）
- 结构改为 flip-card > card-top + card-bottom + card-flap > flap-front + flap-back
- card-top/card-bottom 静态显示新数字上下半（overflow:hidden 裁剪）
- card-flap 上半片 rotateX(-180deg) 绕 bottom center 向下翻，flap-front 旧数字→flap-back 新数字
- 动画后 DOM querySelector + transition:none 复位翻片
- 版本：2026.7.26-27 → 2026.7.26-28

## 修翻页动画+左对齐+计时时隐藏风格（v2026.7.26-27）
- 翻页：setTimeout 内同步 prevTimeChars=[...timeChars]，动画结束后正反面一致
- 左对齐：.mode-row 加 width:100%，撑满 break 父容器 align-items:center
- 计时隐藏：风格 toggle 加 v-if="timerState==='idle'"
- 版本：2026.7.26-26 → 2026.7.26-27

## 计时器风格切换：翻页/简约/LED + 持久化（v2026.7.26-26）
- settingsStore ExtendedUserSettings.focus 加 timerStyle: 'flip'|'plain'|'led'，默认 flip
- 风格选择器 UI：三个小按钮在模式标签同行下方
- 翻页时钟：CSS 3D rotateX 卡片翻转动画，perspective 300px，0.6s cubic-bezier
- LED 数码：Courier New 琥珀色 #fbbf24 + 三层 text-shadow 模拟发光
- displayTime watch 改为 immediate:true，自动维护 timeChars/prevTimeChars/flippingChars
- 删除 4 处冗余的 timeChars 手动初始化
- 版本：2026.7.26-25 → 2026.7.26-26

## 模式标签与按钮分两行（v2026.7.26-25）
- .mode-row flex-direction: row→column, align-items: flex-start, gap 12→6px
- 版本：2026.7.26-24 → 2026.7.26-25

## 时序调整+去设置+动态颜色+布局重组（v2026.7.26-24）
- 去 focusName 的 clearable
- 新增 timerColor computed：番茄钟绿→红（按剩余比例），正计时白→蓝（2h渐变）
- type-toggle 移到 timer 下方+input 上方，加"模式"标签左对齐
- 番茄时长 el-input-number 内联到事项名称下方，删设置按钮+弹窗（模板/script/CSS）
- .focus-info-card 去 background/border
- 新增 input-number 暗色样式
- 版本：2026.7.26-23 → 2026.7.26-24

## 模式切换移到按钮行 + iOS 滑动胶囊动画（v2026.7.26-23）
- type-toggle 从页面顶部移入 focus-start-wrapper，与设置/开始按钮同行
- 新增 .toggle-slider 滑动胶囊指示器，纯 CSS 动画（left 过渡 0.35s cubic-bezier）
- active-right class 控制滑块在左右两半之间切换
- 版本：2026.7.26-22 → 2026.7.26-23

## 间距调整+去发光+去标签+加label（v2026.7.26-22）
- type-toggle margin-bottom 16→32px, timer-time 加 margin-bottom 48px
- timer-time 去 text-shadow
- 删标签输入框、tagChips、parseTags、fav-tags 显示，lastCompletedFocus/saveAsFavorite 去 tags
- 输入框上面加 .input-label（事项名称/备注），13px rgba(255,255,255,0.65)
- 版本：2026.7.26-21 → 2026.7.26-22

## 专注页面全方位布局优化（v2026.7.26-21）
- 需求：模式切换移到顶部/按钮相连丝滑切换、计时器去装饰、输入框改多行+去字数限制+标签、页面左右24px padding、卡片响应式列数
- 实现：
  - 类型切换从 content 内移到 container 顶部，按钮重叠无间隙+overflow hidden，cubic-bezier 过渡
  - timer-time 去圆形装饰，保留72px字号+text-shadow
  - 输入框改 type="textarea" autosize，去 maxlength/show-word-limit，新增标签输入+chip显示
  - FavoriteFocus 接口加 tags 字段，saveAsFavorite/lastCompletedFocus 同步更新
  - 卡片间距24px，ResizeObserver 按公式计算列数（300+k*150+k*24 阈值）
  - 页面 padding: 0 24px
- 涉及：FocusTimer.vue、focusStore.ts、guideContent.ts
- 版本：2026.7.26-20 → 2026.7.26-21

## 优化专注页面布局（v2026.7.26-20）
- 需求：垂直居中 + 时间更大 + 加装饰
- 实现：CSS only
  - .focus-container justify-content: center 垂直居中
  - .timer-time 字号 38→72px，圆形边框+径向渐变背景+box-shadow 光晕
  - .type-toggle margin 微调
- 版本：2026.7.26-19 → 2026.7.26-20

## 移除专注页面 3D 场景，回归纯 2D 计时器（v2026.7.26-19）
- 需求：移除地球和太阳，完全去掉 3D 场景
- 方案：用户选择方案C（完全去掉 3D 场景，回到纯 2D 计时器界面）
- 实现：
  - FocusTimer.vue 移除 FocusTimerScene 组件引用和 import
  - 移除为 3D 场景添加的 position/ z-index CSS
  - 删除 public/textures/earth.jpg 和目录
  - guideContent.ts 专注模块描述改回纯 2D 文字
- 版本：2026.7.26-18 → 2026.7.26-19

## 专注页面 Three.js 地球公转/自转可视化（v2026.7.26-18）
- 需求：专注页面倒计时用 three.js 做地球绕太阳公转（公转进度=倒计时进度），正计时改成地球自转
- 方案：用户确认方案A（完全用3D替代SVG沙漏/无限环）+ NASA真实纹理
- 实现：
  - 重写 FocusTimerScene.vue（491行→280行）：番茄钟模式显示太阳+地球公转轨道，progress 0→1 映射轨道角度 0→360°；正计时模式地球居中自转
  - 太阳纹理程序化生成（Canvas：渐变+亮斑+黑子），地球纹理使用 NASA Blue Marble 2048px 卫星图（public/textures/earth.jpg）
  - FocusTimer.vue 移除 SVG 沙漏/无限环，引入 FocusTimerScene 作为背景层，CSS 叠层正确
  - 更新 guideContent.ts 使用指南中专注模块的视觉描述
- 版本：2026.7.26-17 → 2026.7.26-18

## 全局导航栏收起/展开功能（v2026.7.26-1）
- 需求：桌面端左侧导航栏支持收起为图标窄条、展开回完整宽度，状态持久化
- 方案：方案A（图标窄条，60px）+ 持久化到用户设置 sidebarCollapsed 字段
- 改动文件：
  - `src/stores/settingsStore.ts`：ExtendedUserSettings 接口与 defaultSettings 新增 `sidebarCollapsed: boolean`（默认 false）
  - `src/components/common/nav/MainNav.vue`：新增 `collapsed` prop 与 `toggle-collapse` emit；底部新增折叠按钮（Fold/Expand 图标，仅 left 变体）；CSS 增加 `.nav-left.nav-collapsed` 收起样式（宽 60px、隐藏标题与 label、图标居中）
  - `src/App.vue`：派生 `sidebarCollapsed` computed，传给左侧 MainNav，`toggleSidebarCollapse` 调用 `settingsStore.updateSettings({ sidebarCollapsed })` 持久化
- 发布流程：package.json → 2026.7.26-1；guideContent 增补「全局导航」小节；CHANGELOG 顶部插入 2 条；项目规范.html 更新 MainNav 组件说明（记录 collapsed prop / toggle-collapse 事件 / sidebarCollapsed 字段）
- 验证：vue-tsc 类型检查无新增错误（已有报错均为项目历史问题，与本次无关）
- 注意：vite build 因沙箱拦截 dist/assets 写入被用户拒绝，未跑完整构建；类型检查已通过

## 专注页计时器：沙漏/莫比乌斯环（v2026.7.26-3）
- 需求：按参考图，番茄钟模式用沙漏（hourglass），正计时模式用莫比乌斯环（Möbius strip）
- 用户确认：①沙漏倒计时进度用「沙子下落动画」（上半梯形收缩 + 下半梯形扩张）②时间文字放在「形状几何中心」③莫比乌斯仅「光纹流动」不旋转（保证时间可读）
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 模板：移除 `.timer-ring-wrapper`/`.timer-ring-galaxy`/`.timer-ring-svg`/`.timer-ring-center`/`.timer-time` 整块圆环+星云结构，新增 `.timer-shape-wrapper`（280×320）内用 v-if 切换：
    - **沙漏**（pomodoro）：viewBox 0 0 200 280，8 顶点八边形带 120×40 颈部；2 条 path（外发光描边 + 主体）+ 2 条 `:d` 绑定的沙子梯形（topSandPath / bottomSandPath）
    - **莫比乌斯**（stopwatch）：viewBox 0 0 320 200，4 段三次贝塞尔拼成的双纽线（lemniscate ∞）；3 条同心 path（外光晕 46px + 主体 32px + 内星系纹理 12px），6 个白色 circle 星点；`linearGradient` 内嵌 SMIL `<animate attributeName="offset">` 实现 4s 周期光纹流动
    - 时间文字 `.shape-time` 用 `position:absolute; top/left:50%` 居中；`.hourglass-time` 修饰类降到 18px（颈部只有 120px 容纳 8 字符 hh:mm:ss）
  - 脚本：删除 `galaxyStars`/`RING_CIRCUMFERENCE`/`progressOffset`（不再被新结构使用）；新增 `sandProgress`（已流逝比例 0→1）/`topSandPath`/`bottomSandPath` 三个 computed，按沙漏梯形顶点公式计算 `d` 属性
  - 样式：删除 `.timer-ring-*`/`.galaxy-star`/`.ring-track`/`.ring-progress`/`.star-breathe` 等旧类；新增 `.timer-stage`（320×360）/`.timer-shape-wrapper`/`.shape-svg`/`.shape-time`/`.shape-time.hourglass-time`；`.shooting-stars` 位置微调到左/右上/左下角
- 沙漏坐标推导（viewBox 0 0 200 280）：
  - 主体 path: `M 10 20 L 190 20 L 160 120 L 160 160 L 190 260 L 10 260 L 40 160 L 40 120 Z`
  - 上半梯形顶 yLevel = 120 − 100·progress；左右边按 `10 ± 30·(yLevel−20)/100` 收缩
  - 下半梯形顶 yLevel2 = 160 + 100·progress；左右边按 `40 ∓ 30·(yLevel2−160)/100` 扩张
  - 校验：progress=0 上满下空（yLevel=120 全填、yLevel2=160 退化为线）；progress=1 上空下满
- 发布流程：package.json → 2026.7.26-3；guideContent 专注小节整体重写为沙漏+莫比乌斯描述；CHANGELOG 顶部插 3 条；项目规范.html 未涉及
- 验证：SFC 块结构平衡（template 1 开 + 1 插槽关 / style 1 / script 1）；功能逻辑零改动（开始/取消/完成、跨日分割、状态恢复、可见性同步等全部沿用上一版）
- 风险/注意：v-if 切换 hourglass/mobius 时两个 SVG 的 `id` 相同（`hourglassStroke`/`hourglassFill`/`sandFill` vs `mobiusStroke`），但因 v-if 互斥同一时刻只渲染一个 SVG，id 不冲突；时间显示字体在沙漏模式为 18px（小），莫比乌斯模式 40px（大）

## 专注计时器 three.js 重制（v2026.7.26-4）
- 需求：删除专注页两种模式的 SVG 计时器，用 three.js 重设计（番茄钟→星空沙漏，正计时→银河无限环，各有参考图）
- 用户确认：①计时舞台两视图共用同一个常驻 canvas（切换不闪断）②移除舞台四周 CSS 流星（星空由 3D 场景自带）③时间数字保留现有 HTML 覆盖层（不进 3D）
- 改动文件：
  - **新增 `src/components/focus/FocusTimerScene.vue`**（约 480 行）：props `type/progress(0~1)/running`；
    - 番茄钟：LatheGeometry 玻璃罩（bulbR 半径曲线 r=0.16+1.16·(|y|/2.3)^1.55）+ BackSide 加法混合轮廓光；上下球各 2 层星云粒子云反向旋转；上/下沙堆各 1100 粒子按高度升序生成、`setDrawRange` 随进度伸缩；130 粒颈部流沙（running 且 progress<1 时可见，每帧下落循环）；颈部辉光 Sprite；相机 z=7.2
    - 正计时：自定义 Lemniscate 3D 双纽线曲线（z=0.34·sin2u 穿插）+ TubeGeometry 环身（程序化 Canvas 银河纹理，offset.x 偏移制造光纹流动）+ BackSide 光晕 + 1800 星尘粒子带 + 26 节点最近邻星座连线；摆动动画 `rotation.y = sin(t·0.25)·0.55`（避免侧视丢失 ∞ 轮廓）；相机 z=7.8
    - 公共：700 星点星空背景、RAF 循环（performance.now 手动 dt，**不用 THREE.Clock**——0.184 已弃用）、ResizeObserver、onDeactivated 停循环、unmount 全量 dispose
    - 类型坑：@types/three 0.184 中 `THREE.Curve` 构造函数为 protected，派生类需显式声明 `constructor() { super() }`
  - **`src/components/focus/FocusTimer.vue`**：删除两段重复 SVG（~260 行）与 `topSandPath`/`bottomSandPath`、`.shooting-stars`/`.shape-svg`/`.timer-shape-wrapper` CSS；type-toggle 移出视图（`margin: 24px auto 16px; width: fit-content`），舞台共用（`.timer-stage:first-child { margin-top: 24px }` 处理计时无视图顶部间距），idle/timing 视图 padding-top 归零；计时逻辑零改动
- 发布流程：package.json → 2026.7.26-4；guideContent 专注小节主视觉描述更新；CHANGELOG 顶部插 4 条；项目规范.html 未涉及（**遗留问题**：其「3D 可视化」卡片仍写"Three.js 渲染地球、月球、太阳"，代码中并无此实现，已告知用户未擅改）
- 验证：vue-tsc 通过（FocusTimer.vue 2 个历史报错不动）；vite build 成功（临时 outDir 规避沙箱 bulk-delete 拦截）；agent-browser 实测：沙漏 idle 上满下空、1 分钟番茄全程（沙堆消长+流沙+自动完成+提醒弹窗）、正计时银河环正面摆动计时、CHANGELOG 弹窗显示新条目
- 环境坑：沙箱 safe-delete 把 vite 依赖优化缓存删除转成回收站操作导致 dev 服务器崩溃（three 新依赖触发重优化）；解法：PowerShell 删 `node_modules/.vite/deps_temp_*` 残留 + 沙箱外重启 vite

## 专注页计时器改用纯数字（v2026.7.26-5）
- 需求：删除 3D 场景（FocusTimerScene.vue），保留两种计时模式（番茄钟 + 正计时），仅显示大号发光数字
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 模板：移除 `<FocusTimerScene>` 组件和 `.timer-stage` 包裹，替换为 `.digital-timer` 单行大数字显示
  - 脚本：移除 `import FocusTimerScene`、`sandProgress` computed、空 `watch(focusType)`；保留全部计时逻辑（倒计时/正计时/跨日分割/状态恢复）不动
  - 样式：`.timer-stage` / `.shape-time` / `.hourglass-time` 替换为 `.digital-timer`（48px monospace 发光文字，居中）
- FocusTimerScene.vue 文件本身未删除（遵循编码规则"不动原本就存在的无效代码"）
- 发布流程：package.json → 2026.7.26-5；guideContent 跳过（仅视觉变更）；CHANGELOG 顶部插入 1 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页计时器重新设计（v2026.7.26-2）
- 需求：按参考图重做专注页计时器样式（紫罗兰发光圆环 + 环内星云 + 中央时间 + 环边进度弧），同时优化整页布局
- 用户确认的关键决策：
  - 进度显示：环边进度弧（SVG circle stroke-dashoffset，倒计时随时间缩减）
  - 时间格式：保持动态（mm:ss / hh:mm:ss / dd:hh:mm:ss）
  - 装饰：移除原中央 🌍 地球 + 🌙 月亮轨道动画
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 模板：用 `.timer-stage > .timer-ring-wrapper` 替换原 `.star-ring-container`；环内 `.timer-ring-galaxy` 用多层 radial-gradient 模拟紫色星云；32 颗 `.galaxy-star` 随机分布在半径 20-46% 区域闪烁；SVG `<circle r=96>` 作为进度弧，stroke 用 `url(#ringGradient)`（#a78bfa→#c084fc→#f0abfc 渐变）+ drop-shadow 发光；中央 `.timer-time` 显示时间，56px monospace + 紫色 text-shadow
  - 类型切换按钮改为胶囊式 `.type-toggle`，放在圆环上方
  - 计时中状态：原 `focus-info-inline`（name:time 一行）改为 `.focus-info-card` 卡片（只显示 name + notes，时间已在环内）
  - 周边流星：3 条 `.shooting-star` 用 linear-gradient + translate/rotate 动画
  - 配色：按钮/输入框 focus 态从原蓝色 `#667eea` 改为紫罗兰 `#a78bfa`
  - 脚本：删除旧 `starRingStars`/`stopwatchStars`/`moonOrbitAngle`/`moonOrbitStyle`/`stopwatchMoonStyle` 及常量 `STAR_COUNT`/`RING_RADIUS`/`RING_CX`/`RING_CY`/`RingStar`；新增 `galaxyStars`（一次生成）和 `RING_CIRCUMFERENCE`；`progressOffset` 改用 r=96 周长，并加 `timerState === 'running'` 守卫（idle 时满环）
  - 保留原 `timeChars`/`prevTimeChars`/`flippingChars`/空 `watch(focusType)` 等历史死代码不动
- 发布流程：package.json → 2026.7.26-2；guideContent 专注小节把「星轨环动画」改为「星云圆环 + 进度光弧」描述；CHANGELOG 顶部插入 4 条；项目规范.html 未涉及（无组件规范/数据路径/架构变更）
- 验证：vue-tsc 类型检查 FocusTimer.vue 仅 2 个报错（`provide` 未使用、`lastCompletedFocus` 可能 null），均为原文件历史问题，本次未引入新错误；SFC 块结构平衡；vite dev 服务器启动正常（HTTP 200）

## 专注页计时器：沙漏 + 无限符号 SVG（v2026.7.26-6）
- 需求：番茄钟模式用沙漏 SVG（沙堆随进度动态），正计时模式用 ∞ 无限符号 SVG（旋转+呼吸动画），数字时间叠在形状中央
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 模板：`.digital-timer` 替换为 `.timer-shape-wrapper`，内含两个 v-if 切换的 SVG（沙漏 vs 无限符号）+ `.shape-time` 时间叠加层
  - 沙漏 SVG（viewBox 0 0 200 280）：八边形玻璃外壳（双层描边模拟发光）+ `linearGradient` 沙子渐变 + 两个 `:d` 绑定的 `<path>`（上沙堆收缩、下沙堆增长）
  - 无限符号 SVG（viewBox 0 0 240 120）：四段三次贝塞尔拼成的双纽线路径 + 双层描边 + CSS 动画（`infinity-spin` 12s 旋转 + `infinity-pulse` 3s 呼吸光晕）
  - 脚本：新增 `sandProgress`、`topSandPath`/`bottomSandPath` 三个 computed；计时逻辑零改动
  - 样式：`.timer-shape-wrapper`（260×280 相对容器）、`.shape-time`（absolute 居中 36px）、`.hourglass-time`（17px 收窄避开沙漏颈部）、`.infinity-svg`/`.infinity-ring` 动画
- 沙漏坐标推导（viewBox 0 0 200 280）：
  - 上沙堆 bottomY = 20 + 110·progress；left = 20 + 50·(bottomY-20)/110；right = 180 - 50·(bottomY-20)/110
  - 下沙堆 topY = 260 - 110·progress；left = 70 - 50·(topY-150)/110；right = 130 + 50·(topY-150)/110
  - 校验：progress=0 → 上沙堆满（bottomY=20）、下沙堆空（topY=260）；progress=1 → 上空下满
- 发布流程：package.json → 2026.7.26-6；guideContent 跳过；CHANGELOG 顶部插入 2 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页计时器 SVG 美化 + 布局调整（v2026.7.26-7）
- 需求：①时间数字移到计时器下方 ②沙漏美化 ③∞ 环去掉旋转、改为光点沿轨道移动
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 模板：
    - 时间数字 `<div class="timer-time">` 从 `.timer-shape-wrapper` 内移出，独立放在形状下方
    - 沙漏增强：木框封顶封底（`<rect rx=3>`）、颈部双环（`<ellipse>`）、径向渐变颈部辉光、玻璃渐变填充（`glassFill`）、上下两处高光反射（`<polygon>`）、3 粒落沙粒子（`v-if="timerState === 'running'"` + CSS 下落动画）
    - ∞ 符号：viewBox 改为 0 0 240 140 留天留地；环身保留呼吸脉冲动画；新增 `<circle class="infinity-dot">` 带 `feGaussianBlur` 滤镜，`v-if="timerState === 'running'"` 时通过 CSS `offset-path` 沿环路径巡游；移除整个 SVG 的旋转动画
  - 脚本：无改动（sandProgress / topSandPath / bottomSandPath 沿用上一版）
  - 样式：
    - `.timer-shape-wrapper` 去掉 `position: relative`
    - `.shape-svg` 改为 `display: block`（不再 absolute）
    - 删除 `.shape-time` / `.hourglass-time` 样式
    - 新增 `.timer-time`（38px 居中发光数字）
    - 新增 `.infinity-dot` 的 `offset-path` + `dot-travel` 4s 匀速巡游动画
    - 新增 `.sand-particle` / `.sand-fall` 1.5s 下落淡出动画（3 粒 staggered）
- 发布流程：package.json → 2026.7.26-7；guideContent 跳过；CHANGELOG 顶部插入 2 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏简化 + 收窄（v2026.7.26-8）
- 需求：木框换暖棕色、删除金属环/玻璃填充/高光/颈部辉光、颈部收窄、沙子倒计时滴落逻辑不动
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 模板：沙漏 SVG 从 23 行精简到 9 行——去掉 `glassFill`/`neckGlow` 渐变、玻璃填充路径、2 个高光多边形、2 个金属环椭圆、1 个颈部辉光椭圆；轮廓颈部从 x=70-130（宽 60）收窄到 x=85-115（宽 30）；木框色改为暖棕；落沙粒子微调居中
  - 脚本：`sandProgress` 简化（`focusType === 'pomodoro' ? 0 : 0` → `0`）；`topSandPath` 适配新尺寸（neck left 20→85, diff 50→65, y 范围 20-22→22-108）；`bottomSandPath` 同步（neck 70→85, 130→115, 150→148, 260→258）
  - ∞ 符号无改动
- 发布流程：package.json → 2026.7.26-8；guideContent 跳过；CHANGELOG 顶部插入 1 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏曲线化 + 修复沙不满 + 颈部 10px（v2026.7.26-9）
- 需求：颈部改为 10px、过渡改曲线、修复沙子未塞满上半的 bug
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 模板：
    - 玻璃轮廓改为四段三次贝塞尔曲线连接直线（非八边形折线）：上缘平直→C 曲线收拢到颈部→颈部直线→C 曲线扩张到底部
    - 颈部坐标 x=95-105（宽 10px）
    - 沙子改用 `clipPath` 裁剪 + `<rect>` 滑动方案：新增 `upperBulb`/`lowerBulb` 两个 `<clipPath>`，上沙堆 `<rect y=22 :height="108*(1-sandProgress)">` 由 clipPath 自然贴合曲线轮廓，下沙堆 `<rect :y="258-110*sandProgress" :height="110*sandProgress">` 同理
  - 脚本：删除 `topSandPath` 和 `bottomSandPath` 两个 computed（clipPath 方案不再需要手动计算沙堆多边形）；`sandProgress` 保留（模板直接消费）
- Bug 修复说明：旧公式 `bottomY=22+108*p` 在 p=0 时 bottomY=22 → 沙堆高度为 0（退化）。新方案 rect y=22 height=108*(1-p) 在 p=0 时 height=108（满），p=1 时 height=0（空），逻辑正确
- 发布流程：package.json → 2026.7.26-9；guideContent 跳过；CHANGELOG 顶部插入 1 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏：颈部 5px + 粒状沙子 + 扩散效果（v2026.7.26-10）
- 需求：颈部改为 5px、沙子粒状、滴落到下沙堆表面带扩散
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 模板：
    - 颈部 x=98-102（宽 5px，原 95-105 宽 10px），同步更新玻璃轮廓和两个 clipPath 的所有贝塞尔控制点
    - 沙子填充从 `linearGradient` 渐变改为 `<pattern id="sandPattern">`（8×8 区域内 5 个随机分布的小圆点模拟沙粒纹理），上下沙堆 rect 的 fill 改为 `url(#sandPattern)`
    - 落沙粒子从 3 颗（`.sand-particle`）升级为 6 颗（`.sand-fall f1-f6`），staggered 延时 (0/0.25/0.5/0.75/0.35/0.9s)
    - 新增 5 颗扩散粒子 `.sand-spread d1-d5`，位于下沙堆表面 y=151-159 区域，运行时可见
  - 样式：
    - 落沙动画 `sand-drop`：translateY(-16px→16px) + opacity 1→0，1.4s linear
    - 扩散动画 `spread`：scale(0.3)→scale(1.6)→scale(3) + opacity 0→0.7→0，1.8s ease-out，staggered 延时
  - 脚本：无改动
- 发布流程：package.json → 2026.7.26-10；guideContent 跳过；CHANGELOG 顶部插入 2 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## Bug 修复：保存常用专注报错（v2026.7.26-11）
- 错误：`saveAsFavorite()` 中先执行 `lastCompletedFocus.value = null`，然后 logger 仍读取 `.name` 导致 `Cannot read properties of null (reading 'name')`
- 修复：在 null 之前提取 `savedName` 局部变量，logger 改用 `savedName`（1 行新增 + 1 行修改变量引用）
- 发布流程：package.json → 2026.7.26-11；guideContent 跳过；CHANGELOG 顶部插入 1 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏：金色沙子 + 统一尺寸 + 重力锥形（v2026.7.26-12）
- 需求：沙子颜色换合适、尺寸统一、下半满足重力堆锥形
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 颜色：全紫色系 → 金黄/琥珀系（pattern/落沙/扩散三种共 14 个色值统一为金色调）
  - 尺寸：pattern 5 个圆点 r 全 1.2；6 颗落沙 r 全 1.3；5 颗扩散 r 全 1.2
  - 重力锥形：下半砂堆从 `rect`+`lowerBulb` clipPath（填满球体如液体）改为 `<polygon>` 锥形——4 顶点 neck(98/102, 258-110·p) + 底边 (100±80·p, 258)，p=0 退化 p=1 满锥；删除 lowerBulb clipPath
  - 上半保持 rect+clipPath（重力下沙面平坦是合理的）
- 发布流程：package.json → 2026.7.26-12；guideContent 跳过；CHANGELOG 顶部插入 1 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏：五彩星星 + 上沙堆自上而下 + 下沙堆曲面（v2026.7.26-13）
- 需求：沙子改五颜六色星星粒子；上半从上往下渐变空；下半三角锥改曲面
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - **星星粒子**：`<defs>` 新增 `<path id="star">`（4 尖菱形），`<pattern>` 改用 5 个 `<use href="#star">` 分别着 5 色（金黄 #fac850、柔粉 #fa8cb4、天蓝 #78d2f0、淡紫 #b496e6、暖橘 #f0b43c），scale 0.65-0.8 营造大小层次；落沙/扩散粒子也换 5 色彩色圆点
  - **上半自上而下变空**：`y="22"` → `:y="22+108*sandProgress"`，高度仍为 `108*(1-p)` → p=0 时上沿贴顶满沙，p 增大时上沿下移（顶部先空），底沿始终在颈部——物理正确
  - **下半曲面**：`<polygon>`（三角平面）→ `<path :d="lowerSandPath">`（二次贝塞尔曲面），新增 `lowerSandPath` computed
    - 顶点在颈部 (98/102, cy=258-110p)
    - 右侧 Q 控制点 (100+40p, (cy+258)/2) 拉出外凸弧线
    - 底边 (100±80p, 258)，p=0 退化、p=1 满丘
  - 脚本：新增 `lowerSandPath` computed（7 行）
- 发布流程：package.json → 2026.7.26-13；guideContent 跳过；CHANGELOG 顶部插入 3 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏：银白星星沙子（v2026.7.26-14）
- 需求：星星粒子改银白色沙子；计时前上半满、计时后下半满
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 颜色：pattern 5 颗星从 5 彩（金/粉/蓝/紫/橘）→ 银白系 5 阶灰度微差（`rgba(245,242,255,0.75)` ~ `rgba(210,208,230,0.6)`）；6 颗落沙 + 5 颗扩散粒子同步换银白
  - 上半满/下半满验证通过——`y=22+108p h=108(1-p)` 在 p=0 时 y=22 h=108（满），`lowerSandPath` 在 p=1 时 cy=148 bx=80（从颈部填满至玻璃底 x=20-180）
  - 脚本无改动（公式已正确）
- 发布流程：package.json → 2026.7.26-14；guideContent 跳过；CHANGELOG 顶部插入 1 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏：沙子→液体 + 删扩散（v2026.7.26-15）
- 需求：沙子改成液体，删除扩散效果
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - **液体**：`starPattern`（星星粒子 pattern + star def）→ `liquidGrad`（`linearGradient` 紫蓝渐变），上下填充全换 `url(#liquidGrad)`；下半从曲面 `<path :d="lowerSandPath">` 改回 `rect` + `lowerBulb` clipPath（液体平面填充）
  - **删扩散**：5 个 `sand-spread` 圆点全部删除；CSS `.sand-spread` / `d2-d5` / `@keyframes spread` 块删除（净减少 19 行）
  - **脚本精简**：`lowerSandPath` computed 删除（净减少 8 行）
  - 落沙粒子保留（6 颗银色小滴，运行时可见）
- 发布流程：package.json → 2026.7.26-15；guideContent 跳过；CHANGELOG 顶部插入 1 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏：白色边框 + 银白液体 + 液柱（v2026.7.26-16）
- 需求：边框改白色、液体改银白、滴落改连续液柱（从上液面到下液面）
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 玻璃轮廓：双层 stroke 从紫 `rgba(167,139,250,...)` → 白 `rgba(255,255,255,0.3/0.05)`
  - 液体：`liquidGrad` 从紫蓝渐变 → 银白渐变（`rgba(245,242,252,0.45)` → `rgba(210,205,225,0.15)`）
  - 液柱：6 颗离散落沙圆点 → 1 根 `<rect x=99 y=132 width=2 :height="(258-110p)-132">` 连续细柱，运行时连接上半下表面 (y=130) 到下半上表面 (y=258-110p)，p=0 时全长 126px、p=1 时缩至 16px
  - CSS：删除 `.sand-fall` / `.f2-f6` / `@keyframes sand-drop` 块（净减少 12 行）
- 发布流程：package.json → 2026.7.26-16；guideContent 跳过；CHANGELOG 顶部插入 2 条；项目规范.html 跳过
- 验证：vue-tsc 通过

## 专注页沙漏：紫色边框 + 间断液柱（v2026.7.26-17）
- 需求：边框改回紫色、液柱断断续续、顶部连上半下表面
- 改动文件（仅 `src/components/focus/FocusTimer.vue`）：
  - 玻璃轮廓：双层 stroke 从白 `rgba(255,255,255,...)` → 紫 `rgba(167,139,250,0.35/0.05)`
  - 液柱：1 根实心 solid rect → 5 段 1.5×2.5px 小水滴 rect，y=130/135/140/145/150，5px 间距，`rx=0.75` 圆角，包裹在 `<template v-if>` 中
  - 首段起点 y=130（上半液体下表面，紧贴颈部出口）
- 发布流程：package.json → 2026.7.26-17；guideContent 跳过；CHANGELOG 顶部插入 2 条；项目规范.html 跳过
- 验证：vue-tsc 通过
